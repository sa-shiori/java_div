'use strict';

{
	const minutes = document.getElementById('minutes');
	const seconds = document.getElementById('seconds');
	
	function coundDown(){
		const now = new Data();
		console.log(now);
		const tommorow = new Date(now.getFullYear)
	}
}